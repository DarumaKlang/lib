# lib
Library สำหรับการคคำนวนเลข 7 ตัว

## โครงสร้างไฟล์

- `src/lib/grid-calculator.ts` : เก็บฟังก์ชันคำนวณ (Logic)
- `src/data/grid-variables.ts` : เก็บข้อมูลชื่อตัวแปร (Data)
- `src/components/TriplexGridFinder.tsx` : หน้าจอแสดงผล (UI Component)

*(หมายเหตุ : ถ้าโปรเจกต์คุณไม่ได้ใช้โฟลเดอร์ src ให้เอา src ออกแล้ววางใน root ได้เลยครับ)*

สรุปการนำไปใช้งาน (Usage Guide) ของฟังก์ชันทั้ง 3 ตัวให้เข้าใจง่ายๆ โดยแบ่งตาม **"วัตถุประสงค์"** และ **"สถานการณ์จริงบนหน้าเว็บ"**

---

## 1. `calculateColumnMajor(X)`
**ฉายา:** "นักระบุตำแหน่ง" (The Locator)

* **หน้าที่:** แปลง "ตัวเลขลำดับ" ($X$) ให้กลายเป็น "พิกัด" ($R, C$)
* **หลักการ:** ใช้ตรรกะการเรียงแบบ **แนวดิ่ง (บนลงล่าง)** ในตาราง 3x7
* **Input:** เลขลำดับเดียว (เช่น `50`)
* **Output:** พิกัด `{ row: 2, column: 3, slotIndex: 8 }`

**🎯 สถานการณ์ที่นำไปใช้:**
ใช้เมื่อคุณมี **ช่อง Input ช่องเดียว** ให้ผู้ใช้กรอกเลขลำดับ (1-99) แล้วระบบต้องบอกทันทีว่าเลขนั้นอยู่ตรงไหนของตาราง
* **User:** พิมพ์เลข `50`
* **Code:** `const pos = calculateColumnMajor(50);`
* **UI:** แสดงผล "เลข 50 อยู่ที่ **แถว 2 คอลัมน์ 3**"

---

## 2. `calculateTriplexGridValue(Start..., X)`
**ฉายา:** "นักทำนายค่า" (The Predictor / Forward Logic)

* **หน้าที่:** คำนวณหาว่า "ณ ตำแหน่งของเลข X นั้น... จะมี **ค่าตัวเลข (V)** อะไรปรากฏอยู่" โดยอิงจากค่าตั้งต้น ($Start$)
* **หลักการ:** หาพิกัดก่อน $\rightarrow$ แล้วคำนวณค่าโดยวนรอบ 7 ช่อง
* **Input:** ค่าตั้งต้น 3 แถว (`5, 2, 4`) และ เลขลำดับ (`50`)
* **Output:** ค่าตัวเลขสุดท้าย `{ finalValue: 4, ...พิกัด }`

**🎯 สถานการณ์ที่นำไปใช้:**
ใช้เมื่อคุณต้องการแสดงรายละเอียดลึกขึ้น คือไม่บอกแค่ว่าเลข 50 อยู่ตรงไหน แต่บอกด้วยว่า **"ตรงนั้นมีค่าพลัง/ค่าตัวเลขเป็นเท่าไหร่"**
* **User:** ตั้งค่าเริ่ม [5, 2, 4] และพิมพ์เลข `50`
* **Code:** `const result = calculateTriplexGridValue(5, 2, 4, 50);`
* **UI:** "เลข 50 อยู่ที่ R2C3 และมีค่าเท่ากับ **4**"

---

## 3. `findPositionByFinalValue(Start..., V)`
**ฉายา:** "นักค้นหาเป้าหมาย" (The Finder / Inverse Logic)

* **หน้าที่:** ค้นหาย้อนกลับว่า "ถ้าอยากได้เลข **V** ต้องไปที่ **คอลัมน์ไหน** ของแต่ละแถว?"
* **หลักการ:** คำนวณผลต่าง (Diff) ระหว่างสิ่งที่อยากได้ กับ ค่าตั้งต้น
* **Input:** ค่าตั้งต้น 3 แถว (`5, 2, 4`) และ เลขที่อยากหา (`3`)
* **Output:** รายการพิกัดของทั้ง 3 แถว `[ {R1, C6}, {R2, C2}, {R3, C7} ]`

**🎯 สถานการณ์ที่นำไปใช้:**
ใช้ใน **Mode 2** ที่เราเพิ่งทำ Dropdown List ครับ คือผู้ใช้เจาะจงมาเลยว่า "ฉันต้องการหาเลข 3" ระบบต้องกวาดหาให้
* **User:** ตั้งค่าเริ่ม [5, 2, 4] และเลือก Dropdown `3`
* **Code:** `const list = findPositionByFinalValue(5, 2, 4, 3);`
* **UI:** แสดงรายการ:
    * แถว 1 อยู่ที่ คอลัมน์ 6
    * แถว 2 อยู่ที่ คอลัมน์ 2
    * แถว 3 อยู่ที่ คอลัมน์ 7

---

## 💡 ภาพรวมการเชื่อมต่อ (Workflow)

เพื่อให้เห็นภาพชัดเจนขึ้น นี่คือแผนผังว่า Function เชื่อมโยงกับ Component และ Data อย่างไร :

1.  **User Input :** รับค่าจากหน้าจอ (X หรือ V)
2.  **Process (Lib) :** เรียกใช้ Function 1, 2 หรือ 3 เพื่อคำนวณ
3.  **Lookup (Data) :** เอาผลลัพธ์ (R, C) ที่ได้ ไปดึงชื่อจาก `VariableMap` (เช่น R2C3 คือ "โพงน้ำป่า")
4.  **Display :** แสดงผลลัพธ์ทั้งหมดบนหน้าจอ

ตอนนี้คุณมีเครื่องมือครบทุกมิติแล้ว ทั้ง **ระบุพิกัด (Func 1)**, **ทำนายค่า (Func 2)**, และ **ค้นหาเป้าหมาย (Func 3)**
